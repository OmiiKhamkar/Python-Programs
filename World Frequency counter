text = input("Enter a sentence: ")

words = text.split()
freq = {}

for w in words:
    freq[w] = freq.get(w, 0) + 1

print("\n--- Word Frequency ---")
for word, count in freq.items():
    print(f"{word}: {count}")
